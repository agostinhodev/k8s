apiVersion: v1
kind: Service
metadata:
  name: "node-server-service"
spec:
  selector:
    app: "node-server"
  type: NodePort
  ports:
    - name: "node-server-service"
      port: 80
      targetPort: 3000
      protocol: TCP
      nodePort: 30001
